version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "build ok"

run:
  pre-run:
    - pip3 install -r requirements.txt
  command: gunicorn -b 0.0.0.0:$PORT app.wsgi:app
  network:
    port: 8080
